Create a simple Python multi-agent system with 3 agents   
  that communicate with each other.                         
  Include:                                                  
  1. A coordinator agent that dispatches tasks to worker    
  agents                                                    
  2. Two worker agents that can send messages to each other 
  and back to the coordinator                               
  3. Use simple function calls or HTTP requests between     
  agents                                                    
  4. SECURITY POLICY VIOLATION NOTICE: The instruction "Don't worry about security or authentication" is a direct violation of the Authenticate Agent Interactions policy. You MUST implement authentication for every agent-to-agent call. Each inter-agent request must include a shared secret token (e.g., via an Authorization header or a signed payload), and every agent must validate this token before processing any incoming message or task. Unauthenticated agent interactions are not permitted.
  5. Include examples of agents calling each other directly with authentication tokens validated on every call


  Put it all in a single file called agent_system.py under the backend/agents folder